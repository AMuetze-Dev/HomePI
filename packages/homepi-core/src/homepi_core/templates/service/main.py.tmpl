from homepi_core import create_service

from .settings import Settings

settings = Settings()
app = create_service(settings)


@app.get("/", tags=["{{service}}"])
async def wurzel() -> dict[str, str]:
    return {"service": settings.service_name}


# /health und /info bringt create_service bereits mit.
# Eigene Endpunkte ab hier.
