name: apps

# Die einzelnen Dienste stehen in eigenen Dateien und werden hier eingebunden.
# "homepi new" traegt ein neues Artefakt automatisch in diese Liste ein - es
# sei denn, es laeuft als Modul im Gateway, dann braucht es gar keinen Eintrag.
include:
  - services/gateway.yml
  - services/web.yml
